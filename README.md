# Frogprog configuration

## fully configured linux configuration for my needs.

This is my linux configuration that includes everything that I need (WM, neovim setup etc.).
It is done via amazing tool called stow.
